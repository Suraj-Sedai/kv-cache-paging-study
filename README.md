# KV Cache Paging Study

This project currently provides a cache-operation microbenchmark. It is not yet
a full LLM inference benchmark.

The uniform workload is a control for paging overhead. Variable-length and
staggered workloads are required before making claims about paging usefulness.
